[Startup]
PIDFile = \"${SIM_FOLDER}/temp/${SIM_NAME}.pid\"
regionload_regionsdir=\"${SIM_FOLDER}/Regions\"
DecodedSculptMapPath = "${SIM_FOLDER}/j2kDecodeCache"

[Network]
http_listener_port = ${SIM_PORT}

[DatabaseService]
${DATABASE_STRING}

[XEngine]
ScriptEnginesPath = "${SIM_FOLDER}/ScriptEngines"

[Architecture]
Include-Architecture = \"${ARCHITECTURE}\"
